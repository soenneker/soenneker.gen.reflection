@page "/parameter-pattern"
@rendermode InteractiveWebAssembly

<PageTitle>Parameter Pattern Test</PageTitle>

<h1>Parameter Pattern Test</h1>

<button class="btn btn-primary" @onclick="TestParameterPattern">Test Parameter Pattern</button>

@code {
    private void TestParameterPattern()
    {
        var source = new ExternalProjectDto { Id = "param-test" };
        
        // Pattern: variable with explicit type.Adapt<Type>()
        var dest = source.Adapt<ExternalDestDto>();
    }
}

