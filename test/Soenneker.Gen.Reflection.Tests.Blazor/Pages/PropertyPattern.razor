@page "/property-pattern"
@rendermode InteractiveWebAssembly

<PageTitle>Property Pattern Test</PageTitle>

<h1>Property Pattern Test</h1>

<button class="btn btn-primary" @onclick="TestPropertyPattern">Test Property Pattern</button>

@code {
    private class Result
    {
        public ExternalSourceDto Value { get; set; } = new ExternalSourceDto { Id = "prop-test" };
    }

    private Result _result = new Result();

    private void TestPropertyPattern()
    {
        // Pattern: result.Value.Adapt<Type>()
        var dest = _result.Value.Adapt<ExternalDestDto>();
    }
}


